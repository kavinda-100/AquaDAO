import React from "react";

/**
 * How the DAO works section
 * 1. By Aqua tokens by paying ETH (Become a member of the DAO)
 * 2. Create proposals by someone (the one who created the proposal is the    owner)
 * 3. Others can Vote on proposals (votes for or against)
 * 4. Execute proposals - (Only Owner of the proposal can execute)
 * 5. Governance and transparency
 */

export const HowItWorks = () => {
  return <div>How It Works</div>;
};
